apiVersion: apps/v1
kind: Deployment
metadata:
  name: npm-app
  labels:
    app: npm-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: npm-app
  template:
    metadata:
      labels:
        app: npm-app
    spec:
      containers:
        - name: npm-app
          image: 141559732042.dkr.ecr.us-east-1.amazonaws.com/npm-app-backend:22
          ports:
            - containerPort: 3000
